rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Permite acesso de leitura e escrita para desenvolvimento.
      // Em produção, restrinja estas regras.
      allow read, write: if true;
    }
  }
}
