rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Para fins de prototipagem, permite acesso aberto.
    // Em produção, você deve restringir o acesso.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
