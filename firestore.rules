rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes for prototyping and initial setup.
    // WARNING: This is insecure for a production environment.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
