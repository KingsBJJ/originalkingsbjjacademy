rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // For this prototype, allow all reads and writes to all collections.
    // This is NOT secure for a production application.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
